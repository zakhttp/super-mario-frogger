describe('first test', function() {
    it('should be true', function() {
        rowHeight.should.equal(101);
    });
    it('should be an object', function() {
        rowHeight.should.equal(101);
    });
});
